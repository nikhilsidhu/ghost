export default function DevSettings() {
  return (
    <div class="px-4">
      <span class="text-sm text-[var(--neutral-400)]">
        dev session auto-creates on startup (instance #1 hosts, others join)
      </span>
    </div>
  );
}
